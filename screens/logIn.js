import { Text, View } from 'react-native';
import React from 'react';

export default function LogIn({ navigation }) {
    return (
        <View>
            <Text>
                Log In
            </Text>
        </View>
    );
}